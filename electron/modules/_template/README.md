# Copy this folder to scaffold a new tool module.
# 1. Rename `_template` → your feature id (e.g. `fx`)
# 2. Add `electron/ipc/<id>.ipc.ts` and register in `ipc/index.ts`
# 3. Add renderer `src/features/<id>` + route + locale keys
